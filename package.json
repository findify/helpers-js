{
  "name": "",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tslint --project .",
    "build": "npm run clean && tsc",
    "test": "mocha --compilers ts:ts-node/register --recursive -R min  ./__tests__",
    "release": "npm-run-all -s test clean build lint",
    "preversion": "npm run release && git checkout master && git merge develop",
    "postversion": "git push && git push --tags && git checkout develop && git merge master && git push"
  },
  "author": "1ven",
  "license": "ISC",
  "dependencies": {
    "@types/expect": "^1.13.31",
    "@types/lodash": "4.14.30",
    "@types/mocha": "^2.2.35",
    "@types/redux-logger": "^2.6.32",
    "expect": "^1.20.2",
    "findify-sdk": "https://github.com/findify/javascript-sdk.git#1.1.1",
    "lodash": "^4.17.3",
    "mocha": "^3.2.0",
    "npm-run-all": "^3.1.2",
    "redux": "^3.6.0",
    "redux-logger": "^2.7.4",
    "rimraf": "^2.5.4",
    "ts-node": "^1.7.3",
    "tslint": "^4.2.0",
    "typescript": "^2.1.4"
  }
}
