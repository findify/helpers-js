describe('search', () => {
  describe('emit', () => {
    // move to generic tests
    it('should send request with "1" page by default');

    it('should not change page if current page is "1" while emitting "prevPage" event');

    it('should not change page if current page is last while emitting "nextPage" event');
  });
});
